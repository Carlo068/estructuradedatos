/** Carlo Alejandro Ruiz OLvera
* 14961
* fecha 13/02/2025
*/
public class CircleArea {
    // Se crea una función que recibe el radio de un círculo y regresa el área
    public double calculateArea(double radius) {
        return Math.PI * Math.pow(radius, 2);
    }
}
